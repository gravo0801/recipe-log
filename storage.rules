rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // 누구나 읽고 쓸 수 있음 (개인 앱이므로)
      // 프로덕션에서는 인증 추가 권장
      allow read, write: if true;
    }
  }
}
